# -*- mode: snippet -*-
# name: deflv
# key: deflv
# condition: t
# --
defmodule $1 do
  use ${Web}, :live_view

  @impl true
  def mount(_params, _session, socket) do
    {:ok, socket}
  end
  
  @impl true
  def render(assigns) do
    ~L"""
    $0
    """
  end
end